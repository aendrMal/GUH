local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local hrp = character:WaitForChild("HumanoidRootPart")
local displayName = ""

local plo

if character and character:FindFirstChildOfClass("Humanoid") then
    displayName = character:FindFirstChildOfClass("Humanoid").DisplayName
end

for i, v in ipairs(workspace.Plots:GetDescendants()) do
    if v.Name == "Username" and v.Text == displayName then
        plo = v.Parent.Parent.Parent.Parent
        break -- stop once found
    end
end


if plo then
    print("Found PLO:", plo)
else
    print("PLO not found!")
end

local ignoreNames = {
    ["Farmer Statue"] = true,
    ["Snail"] = true,
    ["Snail Nest"] = true,
    ["Wooden Trellis"] = true,
	["Horse"] = true,
	["Horse Nest"] = true,
	["Turtle"] =  true,
	["Turtle Nest"] = true,
	["Panda"] = true,
	["Panda Nest"] = true,
	["Red Panda"] = true,
	["Red Panda Nest"] = true,
	["GemDig"] = true,
	["Common Egg"] = true,
	["Uncommon Egg"] = true,
	["Rare Egg"] = true,
	["Legendary Egg"] = true,
	["Mythic Egg"] = true,
}

if plo then
    for _, model in ipairs(plo.Model.Plots:GetChildren()) do
        if model:IsA("Model") and not ignoreNames[model.Name] then
            local success = pcall(function()
                if model.PrimaryPart then
                    model:SetPrimaryPartCFrame(hrp.CFrame * CFrame.new(0, -2, 0))
                else
                    for _, part in ipairs(model:GetDescendants()) do
                        if part:IsA("BasePart") then
                            part.CFrame = hrp.CFrame * CFrame.new(0, -2, 0)
                        end
                    end
                end
            end)
            if success then
                for _, part in ipairs(model:GetDescendants()) do
                    if part:IsA("BasePart") then
                        pcall(function()
                            part.Size = Vector3.new(1, 1, 1)
                            part.CanCollide = false
                        end)
                    end
                end
            end
        end
    end
end
